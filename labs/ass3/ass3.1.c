#include <stdio.h>

int main()
{
    // this code prints the value of the variable input by user
    int a;

    printf("Enter the value of the variable -");
    scanf("%d",&a);

    printf("Value of the Variable is %d",a);
 
    return 0;
}