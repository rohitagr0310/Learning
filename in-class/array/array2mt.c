#include <stdio.h>

int main()
{

    int k, l;
    scanf("%d %d", &k, &l);

    int a[k][l];

    for (int i = 0; i < k; i++)
    {
        for (int j = 0; j < l; j++)
        {
            printf("for i = %d and for j = %d", a[k][l]);
                }
    }

    return 0;
}